const config = {
    accessSecret: "bezkoder-access-secret-key",
    refreshSecret: "bezkoder-refresh-secret-key",
    jwtExpiration: 3600,        // 1 hour
    jwtRefreshExpiration: 86400, // 24 hours
}

export default config;